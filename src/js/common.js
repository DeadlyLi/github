//head注释//鼠标滑过出现下拉菜单和二维码$(function(){	//获取页面元素	var $weixin = $('.header .weixin');	var $wechat = $weixin.find('.wechat');	var $sj = $('.header .sj');	var $phone = $sj.find('.appphone');	var $myYt = $('.header .head_right .myYt');	var $ol  = $myYt.find('ol');	//鼠标进入微信二维码出出现	$weixin.on('mouseenter',function(){		$wechat.show();	}).on('mouseleave',function(){		$wechat.hide();	})	//鼠标进入客户端二维码出出现	$sj.on('mouseenter',function(){		$phone.show();	}).on('mouseleave',function(){		$phone.hide();	})	//鼠标进入我的银泰网下拉框出出现	$myYt.on('mouseenter',function(){		$ol.show();	}).on('mouseleave',function(){		$ol.hide();	})			//nav多级菜单	//获取页面元素	var $moveOL = $('.nav .menu ol');	//鼠标移进出现下级菜单	$moveOL.on('mouseenter','li',function(){		$(this).find('.list').show();		//鼠标移除隐藏下级菜单	}).on('mouseleave','li',function(){		$(this).find('.list').hide();	})	//移进显示导航	var $fen_Lie = $('.nav .menu');	$fen_Lie.on('mouseenter',function(){		$(this).find('.ol').show();	}).on('mouseleave',function(){		$(this).find('.ol').hide();	});	})//倒计时总秒数量//	var now = new Date();
//	now.setDate(now.getDate()+2);	var $TotalSecond = parseInt(100000);		timer($TotalSecond);	function timer($TotalSecond){		window.setInterval(function(){		//声明变量		var day=0,			hour=0,			minute=0,			second=0;//时间默认值			//判断倒计时的秒数		if($TotalSecond > 0){			day = Math.floor($TotalSecond / (60 * 60 * 24));			hour = Math.floor($TotalSecond / (60 * 60)) - (day * 24);			minute = Math.floor($TotalSecond / 60) - (day * 24 * 60) - (hour * 60);			second = Math.floor($TotalSecond) - (day * 24 * 60 * 60) - (hour * 60 * 60) - (minute * 60);		}		//格式优化		if (minute < 10) minute = '0' + minute;		if (second < 10) second = '0' + second;		$('.temai_bom b').html('此价格还有'+day+'天'+hour+'时'+minute+'分'+second+'秒');		$('.join .color h3 dt').html('数量有限，请尽快提交订单！倒计时：'+day+'天'+hour+'时'+minute+'分'+second+'秒');		$TotalSecond--;		}, 1000);	} 		